// let or var is used to declare variables (but let is most prefared)
// working with string leterals
let firstName = "Emmanuel";
let lastName = "Agbosu";
console.log(firstName, lastName);

// working with number leterals
let fisrtNumber = 20;
let secondNumber = 30;
console.log(fisrtNumber + secondNumber);

// working with booleans 
let isMale = true;
let isFemale = false;
console.log(isMale);

// uninitialized varriables in js are set as undifined 
// the null is used to clear data stored in a varriable
firstName = null;
console.log(firstName); // we cam see that the first name is now empty